extends ../layouts/application.pug

block header
  = t('views.statuses.edit.title')

block content
  .container
    .row.justify-content-center
      .col-md-6
        .card.shadow-sm.bg-white.bg-opacity-85
          .card-body.text-dark
            +formFor()(action=route('updateStatus', {id: status.id}) method='post')
              input(name="_method" type="hidden" value="patch")
              +inputPlain(status, 'name', errors)(type ="text", placeholder=t('views.statuses.new.name'))
              +submit(user)(value=t('views.statuses.edit.submit'))
              